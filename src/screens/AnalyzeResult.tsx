import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function AnalyzeResult() {
  return (
    <SafeAreaView>
      <Text>분석이 완료되었어요!</Text>
    </SafeAreaView>
  );
}
